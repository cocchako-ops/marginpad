node_modules/
**/node_modules/

# collector runtime data (DB, backups, logs) — never commit; aggregates are backed up separately
collector/data/
collector/backups/
collector/logs/

*.log
.env
.env.*
!.env.example

# cloudflare / wrangler
.wrangler/

# scratch screenshots used during development
_*.png

# os
Thumbs.db
.DS_Store

# build output — deployed via wrangler, not git (regenerate with: node build/build.js).
# dist/assets + hand-maintained pages (rekt, rewards, alerts, coins, news, sw.js, …) ARE tracked —
# they are sources, not build output (force-added; a NEW hand-made dist page needs `git add -f`).
dist/*
!dist/assets

# local-only project docs & keys (repo is PUBLIC — never commit these)
CLAUDE.md
ADMIN_KEY.local.txt
screener_prompt.md

# local dev scratch
scratchpad/
shot.cjs
chartsdd.png
pt.png
New Text Document.txt
Bug - screenshots & videos/

# marketing assets (large binaries, not source)
ads/
marginpad-ad-*.png
marginpad-promo-*.png
extension/marginpad-extension.zip
.dev.vars

restore_tlm_whyme.txt

# local secret files (watcher token, admin key, etc.) — NEVER commit (public repo)
*.local.txt
promo/

# private launch prep (never commit)
drafts/
CLAUDE-archive.md

# loose scratch left in the repo root — kline dumps, code fragments, and addr.txt, which holds
# wallet addresses and must never reach this public repo
_t.txt
addr.txt
kl_btc.json
liq
NUL

# throwaway probes written at the repo root during a session
scratch-*.js
scratch-*.png

# ops E2E screenshots (build/ops-e2e.js)
build/ops-shots/

# mobile paper-trade terminal E2E screenshots (build/pt-mobile-e2e.js)
build/pt-shots/

# Vault cosmetics E2E screenshots (build/vault-shots.js)
build/vault-shots/

# Spanish translation work files (transient; the catalog is the source)
build/data/es/chunks/
build/data/es/leftover.json

build/signal-gate/klines.json
build/signal-gate/scan-trades.json

# build artefacts from publish-sdk.js - never commit these
sdk/python/dist/
sdk/python/build/
sdk/python/*.egg-info/

# Browse drawer E2E screenshots (build/browse-e2e.js / shots)
build/browse-shots/

# where-to-start E2E screenshots
build/wts-shots/
